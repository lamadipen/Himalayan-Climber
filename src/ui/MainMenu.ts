// src/ui/MainMenu.ts
// UI/UX Designer entry-point for the main menu scene.
// Full implementation lives in src/scenes/MainMenuScene.ts where GameLoop.ts
// can find it by the conventional scenes/ path — this file re-exports it so
// the spec's src/ui/ location is also valid for imports.
export { MainMenuScene } from '../scenes/MainMenuScene'
